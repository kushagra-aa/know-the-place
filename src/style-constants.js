export const colors = {
  primary1: "#171d28",
  primary2: "#5800ff",
  primary3: "#01f5ea",
  secondary: " #006f9b",
  accent: "#ffc600",
};

export const createBorder = (width, color) => {
  const boder = ` ${width} solid ${color}`;
  return boder;
};
